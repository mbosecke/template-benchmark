<html>
	<head>
		<title>{{title}}</title>
	</head>
	<body>
		{{#projects}}
			<a href="{{url}}">{{name}}</a>
			<p>{{description}}</p>
		{{/projects}}
		{{^projects}}
			No projects
		{{/projects}}
	</body>
</html>
